# CMake out-of-source build directory
build/
build-debug/
build-rel/

# compiled binaries (now produced under build/)
/server
/client

# RDB persistence files
dump.rdb
dump.rdb.tmp
dump.rdb.bak

# AOF persistence files
appendonly.aof
appendonly.aof.tmp
appendonly.aof.bak

# stress-test output (stress_test.py --log)
stress_results.md

# Test suites: only scripts/stress_test.py is tracked. The rest are local-only
# until they are folded into it — see BACKLOG.md → V11.
scripts/test_*.py
scripts/test_*.sh
scripts/test_*.cpp
scripts/diag_*.sh
scripts/myred_testlib.py

# Python bytecode cache
__pycache__/
*.pyc

# editor / IDE
.vs/
.vscode/c_cpp_properties.json

tls/
# local-only: extracted Claude session transcripts
.chatlog/
